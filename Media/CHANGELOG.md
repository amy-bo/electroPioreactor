# electroPioreactorGasModel – changelog

Done-work audit for `electroPioreactorGasModel.xlsx`. Newest first. Open work is in `TODO.md`.

## 2026-06-27

- [x] **Six chlorine/chemistry bug-fixes from the Exp-2 death review** — the 23-agent review (2026-06-26) found the germicidal-chlorine block was self-contradictory and the death's likeliest cause (electrode corrosion / metal leaching) was unmodelled. Fixed, in both the workbook (Chemistry rows + new 148–163) and the Python twin: **(1) chloride mass balance** — `NH2Cl` was producing 17.0 mg/L combined chlorine from a chloride pool of only 9.45 mg/L (1.8× more chlorine than chloride exists); the `MIN()` cap was on ammonium (504 mg/L, never binds) not chloride. Added `Cl_pool_mgL = pH_Cl·52460` as the true ceiling → NH2Cl pinned to 9.45. **(2) n-electron consistency** — `P_HOCl` divided by a fixed 2 e⁻ (Cl₂ route) even when the binding limit is chloride *arrival* (1 e⁻/Cl⁻); added `n_e_Cl = IF(I_Cl_mt≤I_Cl_FE,1,2)`, correcting a ~2× under-count in the operative regime. **(3) double pH-discount** — `C_eff` multiplied `HOCl_ss` (already the undissociated acid) by `pH_fHOCl` a second time; dropped. **(4) C_eff as a range** — added `tau_NH2Cl_lo/hi`, `R_chloramine_lo/hi`, `NH2Cl_lo/hi`, `C_eff_lo/hi`, and a dedicated C_eff sweep in the sensitivity script (the four knobs `tau_NH2Cl`/`R_chloramine`/`km_Cl`/`FE_CER` were never swept). Net effect: **C_eff 0.68 → 0.38 mg/L**, and the full knob envelope is **0.02–0.53 mg/L — the chloride-pool cap holds chlorine below the 2.0 kill threshold across the entire knob box**, so chlorine cannot sterilise in UdG (it was identical, 0.379 vs 0.380, between the survived and died runs anyway). **(5) honest C_eff verdict** — the `bleach_flag` text no longer implies chlorine explains the death; it states it's pool-bounded and sub-lethal and points to the metal pathway. **(6) electrode-corrosion / dissolved-metal placeholder** — added `corr_I_frac` (DATA GAP, pink — measure dissolved Cu/Ni/Fe; the green anode implicates Cu/Ni ammine), `metal_MW`/`metal_z`/`metal_leach_rate`/`metal_tox_thresh`/`metal_flag`: the unmodelled prime suspect is now visible in the sheet, #N/A until measured. Also corrected the **MC02 SID** charge-balance in the twin (Mg²⁺/Ca²⁺ were missing from the cation sum while their SO₄²⁻ was subtracted — the workbook D36 was already correct; UdG unaffected). Reality gate intact (**run 1 → pH 7.11 survived, run 2 → pH 6.41 died**); twin 84/84 → **89/89** (5 new terminal outputs). Workbook edited by raw OOXML surgery under the hardened gate (column-order, calcChain dropped, fullCalcOnLoad set); needs an Excel open+save to regenerate caches.

## 2026-06-26

- [x] **CO₂↔pH coupling, v3 — closed-headspace two-compartment model that reproduces BOTH Angella runs** — the previous atmosphere-decay sawtooth (d3ab82d) failed the only test that matters: Angella *measured* pH ~6 at run 2 (1 s/3 min, died) and it predicted ~7.5. The missed physics: the vial is nearly closed — only ~2.6 mL headspace over 15 mL liquid — so CO₂ sparged in fills the headspace and leaves only through a **slow restricted vent**, and the liquid tracks the CO₂-rich headspace, not the atmosphere. Replaced with a verified two-compartment limit-cycle (Mass Transfer rows 138–164, net +9 named cells; CO2aqc_ss → Chemistry D5): liquid relaxes to saturation during a pulse, then chases the slowly-venting headspace in the gap. Derived + reality-checked by a fresh 3-model adversarial programme; the open-membrane vent (~134 mL/min, clears in seconds) was **rejected** because it can't reproduce the death — the real path is a restricted bleed `Q_vent ≈ 0.5 mL/min` (τ_hs ≈ 5 min). **Reproduces both runs structurally** (not tuned): **run 2 (1 s/3 min) → pH 6.41 (died ✓)**, **run 1 (2 s/60 min) → pH 7.11 (survived ✓)** — the 3-min gap is always below the headspace clearing time, the 60-min gap always above. New default (recommended schedule) → **pH 7.38, in the HOB band**; C_eff ~0.68 (chloramine-dominated, ~pH-independent). Boundaries 6.35 (full sparge) / 7.92 (no sparge). A `vent_floor_chk` guard confirms the vent is the slow path. **`Q_vent` ≈ 0.5 mL/min is the key new uncertain parameter — measure it.** Twin re-baselined; 84/84.

- [x] **pH ↔ sparge-schedule coupling — dissolved CO₂ (and pH) now respond to the schedule** — `CO2aqc`, the dissolved CO₂ the charge-balance pH solve consumes, was a fixed full-sparge saturation, so pH never moved with the schedule. Replaced with a verified **dual-kLa sawtooth limit-cycle** (Mass Transfer rows 138–155, 18 named cells): CO₂ relaxes toward saturation during each pulse (bubble-path kLa) and toward air-equilibrium between pulses (surface kLa), both scaled from the O₂ kLa by √(D_CO2/D_O2); the bounded cycle time-average drives Chemistry D5. Derived + adversarially verified by a 3-model programme (sawtooth chosen because only ~5%% of each pulse dissolves at this reactor's 4 mm bubbles / 17 ms contact, so the simple mass balance over-credits ~4×); a missing `t_off≥0` guard (which would make a more-than-full-sparge schedule report the no-sparge pH) was caught and fixed. **Regime shift (correct physics):** the recommended sparse schedule never saturates, so the default endpoint pH moves **6.31 → ~7.94** and the pH-band flag flips to “above optimum”; 6.31 is real only at near-continuous sparging. Boundaries exact (full sparge → 6.31, no sparge → 7.96); pH crosses the 6.5–7.5 HOB band near a 1–2 min interval. **Honest finding:** pH responds strongly but **C_eff stays ~0.68 across the sweep** — the biocidal chlorine is dominated by the pH-independent monochloramine load, so the schedule's lever is pH, not chlorine (chlorine's levers are medium + current). Mid-range pH accuracy is gated by the unmeasured `kLa_surf` (improvement #1) and the surface-strip-limited assumption. Also: **Summary D14/D16 fill blue when overridden** by the G13/G15 setting. Python twin re-baselined; 84/84.

- [x] **What-if now actually works on the Summary (point 6 done properly)** — the G13/G15 override was wired to hidden Mass-Transfer cells, but **nothing on the Summary changed** when you typed values: the headline D14/D16 showed the recommendation and the checks were binary pass/fail that stayed OK. Fixed three Summary formulas: **D14/D16 now display your entered duration/interval** (override, else the rounded recommendation), and **D26 “CO₂ supply : demand” shows the ratio as a moving number** (e.g. ×2.0 → ×45.4 for 100 s / 1000 min) while keeping the OK/SHORTFALL tokens so the conditional formatting still reds a real shortfall. Type into G13/G15 and the headline plus CO₂-sufficiency / pulse-floor / O₂-accumulation recompute. Still deferred: pH/HOCl don’t couple to the actual CO₂ dose (the CO₂ mass-balance). Twin 84/84; an injected-override test confirms D14/D16/D26 move.

- [x] **What-if schedule override (point 6, initial — superseded by the entry above)** — surfaced and finished the sparge-schedule what-if. A **“Your setting” column on the Summary** (G13 duration, G15 interval; blank = recommended) now overrides the schedule, and the schedule-dependent checks — CO₂ supply, pulse-floor, O₂ accumulation, dosing, and the **OD-window check (rerouted from the recommended to the in-use interval)** — respond to it. The recommended values stay in column D, so nothing is lost and nothing has to move into the input block (the “two columns” you intuited). The existing Mass-Transfer Manual path (`sched_mode` + manual cells) is kept; the Summary override takes precedence. Python twin updated; 84/84, and an injected-override test confirms the checks move (interval 178→300 min → pulses/h 0.34→0.20).

- [x] **Bleaching v2 — graded kinetic non-zero model (replaces the over-rosy “≈0”)** — the stage-1 model treated the ammonium sink as instantaneous and perfect (free HOCl ≈ 0), which was as naïve as the FE=1 fantasy it replaced. Rebuilt on real kinetics (Chemistry rows 128–147, 20 named cells): chlorine production capped by the chloride mass-transport limit (so it scales with chloride); a steady **bulk** free-chlorine residual `[HOCl]_ss = P_HOCl/(k₁·[NH₃_free])`, non-zero because only ~0.6%% of ammonium is reactive free NH₃ at pH 7 (k₁ = 4.2×10⁶, Jafvert & Valentine 1992); a longer-lived **combined** chlorine (monochloramine) load ÷ its ~25× lower potency; and an anode boundary-layer enrichment factor. `C_eff` vs a graded sub-lethal onset (0.1) / kill (2). UdG → C_eff ≈ 0.68 mg/L = **MILD sub-lethal penalty** — non-zero and real (matches Sydow 2017 elsc.201600252, which attributes the chloride penalty to anodic Cl₂ and grows lag-free chloride-free), not sterilising (UdG runs for days); SEVERE only at high chloride/current (Baek 2021 cej.2020.126742). Uncertain knobs (efficiency plateau, f_local, monochloramine residence) exposed as flagged sensitivity inputs — the robust claim is the graded shape, not a precise absolute. Summary D36 → C_eff. FE=1 cells kept, relabelled “naïve, NOT operative”. Twin mirrors all 20 cells — 84/84.

- [x] **Summary UX sweep** — added the three missing source hyperlinks (E34 → Biology DO check, E35 → Chemistry endpoint pH, E36 → Chemistry free HOCl) so the new check/figure rows match every other row; fixed three mis-styled unit cells (C36/C38/C39 were a one-off purple style s=28 → the canonical s=20). Pulse→sparge wording and the A16 typo were already corrected in-sheet.

- [x] **Sparge interval recommended on the real surface-stripping estimate, not zero** — the “62× fix” had over-corrected: by withholding the kL_surf credit entirely (`O2_src_guard = O2_net_gen`), `spg_int_opt` reverted to the zero-surface O₂-limited number (~2.85 min) — the very value that over-doses CO₂ — even though the sheet’s own DO diagnostics read **SURFACE-HELD** (surface stripping holds DO at ~18% of ceiling). `spg_int_opt` (D91) now recommends the **carbon-limited interval (D112, ~178 min)** when the regime is SURFACE-HELD, with the O₂-limited interval kept as a clearly-labelled **conservative fallback** (applies only if measured kL_surf < kL_surf_crit). `opt_binding` reports “CARBON (surface holds DO)”. Fixed the E87/E89 notes that wrongly claimed to credit the surface while their formulas didn’t. Headline interval **2.85 → 178 min** (Summary D16). An estimated kL_surf > 0 beats assuming zero — measuring it is improvement #1. Python twin updated identically; 83/83.

- [x] **Bleaching model rebuilt — realistic free chlorine, not the FE=1 fantasy** — the old `HOCl_max`/`bleach_flag` assumed every anode electron makes free chlorine (FE=1) and the whole chloride inventory persists, giving ~8.9 mg/L for UdG and red-flagging a medium that runs for hundreds of days in the literature. Replaced with a physically grounded block (Chemistry rows 128–140, 13 new named cells): chlorine current = min(applied × CER efficiency, chloride mass-transport limit `n·F·k_m·[Cl⁻]·A`); production rate; **minus** the ammonium breakpoint chlorine demand (7.6 mg Cl₂/mg N, ×10 practical); → steady free HOCl vs a literature HOB impairment-**onset** threshold (0.05 mg/L; MSC band 0.021–0.39, Murray 2017 — below, not above, the 0.2 WHO kill-all residual). For UdG: chlorine current ~57 µA, production ~3.7 mg/L/h, but the ~996 mg/L ammonium sink keeps free HOCl **≈ 0** — flag reads "no free chlorine", Summary D36 shows 0. Fires only on genuinely adverse inputs (high chloride, low ammonium, high current), so it now maps the usable envelope of each medium. Old FE=1 cells relabelled "naïve, NOT operative" and kept for contrast (the ~40× overstatement). Cited: Vos & Koper (JACS 140:10270 / JEAC 819:260, CER efficiency at trace Cl⁻); Pressley, Bishop & Roan 1972 (ES&T 6:622, breakpoint); Murray 2017 (Ecotoxicology 26:1115, threshold). Python twin extended to mirror all 13 cells — **83/83 match**.

- [x] **Perfection wave: source-verification + twin reconciliation** — a multi-agent adversarial pass re-checked every external constant, citation and hardware-capability claim against primary sources. (1) **Python twin de-staled** — `electroPioreactorGasModel.py` bleach_flag (D109) still gated on the old chloride test and its expected-value literal still read "chloride-free", so the headline "match" was false for that one output; re-keyed to gate on `HOCl_max` (>0.2 RISK / >0.02 watch) exactly like the workbook, now a genuine **80/80**. (2) **Chemistry E109 note** corrected — it asserted "chloride-free assumption, enforced" while the formula now fires BLEACH RISK; rewritten with the flag logic and proper provenance for the 0.2 mg/L floor (WHO minimum free-chlorine residual / lower bound of the 0.021–0.39 mg/L MSC range — a precautionary floor, not an organism MIC). (3) **CaCl₂ E33** — literature 0.01 g/L retained; the hypothesised 10× as-built (0.1 g/L) has zero documentary basis in the repo and is marked UNCONFIRMED, not entered. (4) **Biology Henry notes** reconciled to the live 1.2e-5 O₂ reference (CO₂ ~25×→~28×, H₂ ~1.7×→~1.5×). (5) **Mass-Transfer kL_surf** relabelled Danckwerts→penetration-theory/surface-renewal proxy (Higbie form, t=1/s_renew). (6) **LiteratureMedia** Matassa (2016) DOI corrected from the Microbial Biotechnology review (10.1111/1751-7915.12369) to the Water Research formulation paper (10.1016/j.watres.2016.05.077, WebSearch-verified). (7) Three protocols (gerrit-current, flow-calibration, dissolved-oxygen) advanced checked→reviewed; gerrit-current LED channel un-hardcoded. Workbook edits applied via scratch→validate→promote (x14 dataValidations, conditional formatting and 312 defined names preserved byte-for-byte; only repointed cell string-indices changed; fullCalcOnLoad set). The proposed bleach "discriminator" block was **rejected** — D125/D109 are already pH-aware via the HOCl fraction D104, so it was decorative and would have imported an unconfirmed pH-8 anchor.

- [x] **Sparge O₂-vent guards: proper structural fix + Python twin** — the surface-O₂ best-case proxy (`kL_surf`, ~375% uncertain) was silently disabling two safety guards (`surf_strip` > `O2_net_gen` clamped `duty_O2vent` to 0 and blew `spg_int_max` to ~1.5e8 min). New `O2_src_guard` (Mass Transfer D132 = `O2_net_gen`, surface credit withheld) now sizes both guards; `spg_int_max` corrects to 2.846 min and binds the headline `spg_int_opt` **178 → 2.85 min**. Added an `O2-CEILING` branch to `opt_binding` (D94) — the fix exposed a latent mislabel where the now-binding cap would have reported "O2-FREQ". Added steady-growth sawtooth diagnostics `DO_ss_sawtooth` (D133), `spg_int_regime` (D134) and the paired lag readout `DO_ss_sawtooth_lag` (D135); re-fenced `surf_strip` as a diagnostic feasibility ceiling (E73/E88 notes). Corrected `y_O2_vent` (D75) from a ratio to a true mole fraction `O2_excess/(O2_excess+CO2_supply)`. Registered the four new defined names in `workbook.xml` (localSheetId 5). The independent Python twin (`electroPioreactorGasModel.py`) was brought into the repo and updated to the corrected logic — **80/80 outputs match the workbook within 0.5%**, reproducing the 2.85-min interval and `O2-CEILING` binding.

## 2026-06-25

- [x] **Buffered pH & bleaching model → new Chemistry tab** (uncommitted) — replaced the old "buffer holds pH" assumption (which sized CO₂ dosing to O₂ venting rather than to carbon demand) with a real open-system carbonate+phosphate **charge-balance pH solve**, on a dedicated **Chemistry** sheet (7th tab) — the equilibrium maths is graduate physical chemistry, out of place in Biology and clearer for the third-party electrochemist. Each medium's full ionic recipe is entered as published, sourced cells (g/L + handbook molar masses); Excel derives the strong-ion difference, total phosphate, ammonium and chloride by `media_sel`. pH is the root of the charge residual over a transparent trial-pH grid (no circular reference). Equilibrium constants are **van't Hoff–corrected to the reactor's 30 °C** (same treatment the sheet already uses for Henry constants). The HOCl *fraction* (meaningless alone) was replaced by an **anodic bleaching estimate**: free-chlorine evolution from the Gerrit-Law current at FE=1 (conservative ceiling, since FE can't exceed 1), gated on the medium actually containing chloride — turning the dormant "chloride-free" assumption into a live warning. A note on the pH cell flags that it's the full-sparge trough (a lagging probe may miss it). Computed for UdG: pH 6.31 at full CO₂ sparge (air gives 8.0). (The FE=1 free-chlorine ceiling introduced in this entry was subsequently found unphysical — chlorine evolution from trace chloride is mass-transport-limited to a small fraction of the current, and medium ammonium scavenges free chlorine — and the bleaching model is being rebuilt; see later entries.) Biology trimmed of the relocated model; `media_sel` dropdown repointed to Chemistry; stale `calcChain` dropped (rebuilds on load). Earlier same-day work folded in: corruption fix (duplicate F60/F61 cells from a column insert), MC02 relabel (Sydow→MC02, repo-README sourced), and the UdG-phosphate media option.

## 2026-06-19

- [x] **Experiment-protocol set + index** (`6ac8224`) — eight standalone bench SOPs in `protocols/`, each with an optimal and a budget route and tied to the exact model cell it feeds (vial geometry, surface kLa, faradaic efficiency, dissolved O2 + DO bands, per-reactor flow calibration, knallgas stoichiometry, sinter porosity, Gerrit current). Leverage-ordered `README.md` index; hyperlinked from the Summary improvements rows and the geometry-check row. Methods web-grounded and cited.
- [x] **Release-readiness review fixes** (`30b63be`, `2177e6b`, `472917d`) — from a multi-expert adversarially-verified review (verdict: RELEASE-WITH-FIXES). Calibration guard so an uncalibrated reactor shows "calibrate first" not `#DIV/0!`; dropdowns tightened to exact key columns; "Sinter OOR" now reddens on the right cell; conditional formatting + alert tokens extended; O2 Henry constant corrected to 1.2e-5 (Sander 2023, ceiling 10.7 mg/L); etaF=1 upper-bound banner; `kL_surf_crit` threshold cell; folded-block units, leaked-metadata cleanup; old single-sheet file removed; a geometry-consistency check added (flags the open B1).
- [x] **Selectors moved to the top of Summary** (`5a11ef7`, `b863b5e`) — reactor / electrode / organism / media dropdowns as the first control block; sinter porosity demoted; calibration `Reactor` key repointed.
- [x] **Imports-at-top reorg + Summary unit column** (`4261d25`) — every parameter sheet reordered top-to-bottom (imports, then calc, then lookup tables), all references/ranges/dropdowns/hyperlinks remapped; units moved into the Unit column.
- [x] **Folded the Dosing tab into Mass Transfer** (`2d01dfc`) — colliding names merged to single definitions, no behavioural change; six tabs.
- [x] **Dual schedule regime exposed** (`60960ec`) — O2-limited (~1.4 min) vs carbon-limited (~178 min) intervals both surfaced with a verdict, so neither is presented as definitive while `kL_surf` is unmeasured.
- [x] **Front-page input consolidation** (`e85196e`) — all experiment inputs on Summary (reactor/electrode/organism/media + LED intensity, stir rpm, media volume, temperature, pressure); warnings, ranked improvements, conditional formatting, source hyperlinks.
- [x] **CO2 flows single-table calibration** (`6b2d1ef`, `d0606c0`, `cb5bc24`) — one researcher-extensible table; reactor→latest-date lookup via SUMPRODUCT (replaced version-specific MAXIFS that errored on the user's Excel).
- [x] **Selector wiring + lookup tables** (`cf7812f`, `d82ed97`, `e47b6e0`) — reactor→type→geometry (VLOOKUP across all four types, fixing an NA cascade), electrode→sparger/z_e_ORR, organism→DO thresholds; steady-state DO band check; five lookup tables with dynamic dropdowns.

Earlier single-sheet history (pre-modular: cathodic O2 balance, surface-aeration path, sensitivity analysis, reference audit, gas-generation rates) is in the git log.
